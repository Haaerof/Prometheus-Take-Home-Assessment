# Opt in to Microsoft Testing Platform, the test runner .NET 10 uses in place of VSTest.
# xUnit v3 test projects are self-contained executables that this runner invokes directly.
[dotnet.test.runner]
name = "Microsoft.Testing.Platform"
